(name [for … end])

(trigger
    (tab for))

(only-in [source.matlab, source.octave])

(snippet [for %{1="ii"}=%{2="1"}%{3=":%{4=\"n\"}"}
	%{0}
`if [[ $TM_USE_OCTAVE -ne '0' ]]
	then echo "endfor"
else
	echo "end"
fi`])