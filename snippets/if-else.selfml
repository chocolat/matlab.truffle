(name [if … else …end])

(trigger
    (tab ife))

(only-in [source.matlab, source.octave])

(snippet [if %{1="condition"}
	%{2}
else
	%{3}
%{`if [[ $TM_USE_OCTAVE -ne '0' ]]
	then echo "endif"
else
	echo "end"
fi`}])